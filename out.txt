id: sconto-js6  exit: 0  2026-06-26 09:10:42
----
=== inizializzaRangeSliderScontoAuto: parte del widget $slider (oltre) ===

=== sorgente quotazioni: getScontiConsigliatiTariffeAuto ===
;
}

function getScontiConsigliatiTariffeAuto(salvataggio = false) {
    const scontiConsigliatiTariffe = {};

    if (quotazioni.length > 0 && salvataggio) {
        for (const quotazione of quotazioni) {
            if (quotazione.sconto_consigliato != null) {
                scontiConsigliatiTariffe[quotazione.idtariffa] = quotazione.sconto_consigliato;
            }
        }
    }

    for (const tariffa of fromSliderTariffe) {
        if (tariffa.valore != null && !isNaN(parseFloat(tariffa.valo