id: sconto-js3  exit: 0  2026-06-26 08:40:53
----
=== getScontoConsigliatoMassimoAuto ===
) > 0;
}

function getScontoConsigliatoMassimoAuto(quotazione) {
    const scontoTariffa = normalizzaScontoTariffaAuto(quotazione?.sconto_tariffa);
    if (scontoTariffa !== null) {
        return scontoTariffa;
    }

    const scontoOriginale = normalizzaScontoTariffaAuto(quotazione?.sconto_consigliato_originale);
    if (scontoOriginale !== null && scontoOriginale > 0) {
        retur

=== setValoreScontoTariffaAuto ===
lore);
}

function setValoreScontoTariffaAuto(id_tariffa, valore) {
    const valoreNormalizzato = normalizzaScontoTariffaAuto(valore);
    if (valoreNormalizzato === null) {
        return null;
    }

    const indice = getIndiceScontoTariffaAuto(id_tariffa);
    if (indice === -1) {
        fromSliderTariffe.push({
            id_tariffa,
            valore: valoreNormalizzato
       

=== getValoreScontoTariffaAuto ===
iffa);
}

function getValoreScontoTariffaAuto(id_tariffa) {
    const indice = getIndiceScontoTariffaAuto(id_tariffa);
    if (indice === -1) {
        return null;
    }

    return normalizzaScontoTariffaAuto(fromSliderTariffe[indice].valore);
}

function setValoreScontoTariffaAuto(id_tariffa, valore) {
    const valoreNormalizzato = normalizzaScontoTariffaAuto(valore);
    if (valoreN