id: sconto-js5  exit: 0  2026-06-26 09:09:06
----
=== getScontoConsigliatoMassimoAuto (completa) ===
;
}

function getScontoConsigliatoMassimoAuto(quotazione) {
    const scontoTariffa = normalizzaScontoTariffaAuto(quotazione?.sconto_tariffa);
    if (scontoTariffa !== null) {
        return scontoTariffa;
    }

    const scontoOriginale = normalizzaScontoTariffaAuto(quotazione?.sconto_consigliato_originale);
    if (scontoOriginale !== null && scontoOriginale > 0) {
        return scontoOriginale;
    }

    const scontoQuotazione = normalizzaScontoTariffaAuto(quotazione?.sconto_consigliato);
    if (scontoQuotazione !== null && scontoQuotazione > 0) {
        return scontoQuotazione;
    }

    /* const scontoSlider = getValoreScontoTariffaAuto(quotazione?.idtariffa);
    if (scontoSlider !==

=== normalizzaScontoTariffaAuto ===
 5;

function normalizzaScontoTariffaAuto(valore) {
    const valoreNumerico = parseFloat(valore);
    if (isNaN(valoreNumerico)) {
        return null;
    }

    return parseFloat(valoreNumerico.toFixed(2));
}

function isNomeRent(ragioneSociale = "", nomeRent = "") {
    if (nomeRent === "car") {
        return new RegEx